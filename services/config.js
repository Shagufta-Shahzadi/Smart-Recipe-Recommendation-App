// Base URL for API requests
const BASE_URL = "http://192.168.1.7:5000";

// Authentication keys (if any)
const API_KEY = "YOUR_API_KEY";

// Timeout for API requests (optional)
const REQUEST_TIMEOUT = 5000;

// Export the configuration
export default {
  BASE_URL,
  API_KEY,
  REQUEST_TIMEOUT,
};
